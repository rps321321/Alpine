# Attack Lab is out of v1

v1 proves the Repo Loop: Abliterated Model, localhost Inference Server, one Harness, project-scoped Capabilities. Isolated targets, disposable VMs, and private attack networks are the next architectural frontier after that loop is green. Building the Attack Lab first would make containment theater the blocker for a coding agent that does not exist yet.
